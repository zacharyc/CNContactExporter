#if canImport(SwiftUI) && canImport(Contacts)
import SwiftUI

@main
struct CNContactExporterApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
#endif
